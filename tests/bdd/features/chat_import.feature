# language: zh-CN
功能: 用户主动保存游客聊天
  场景: 完整问答只导入一次并可恢复
    假如用户保留了一份游客完整问答并已登录
    当用户确认保存本地会话并重试相同请求
    那么云端只有一个导入会话且可读取全部问答

  场景: 追加消息和改名后仍可有界核对原始快照
    假如用户保留了一份游客完整问答并已登录
    当原导入已追加很多消息并改名后用户再次核对
    那么核对覆盖原始快照且最近页保持独立

  场景: 确认账号与实际 Cookie 账号不一致时禁止导入
    假如用户保留了一份游客完整问答并已登录
    当确认账号 A 后共享 Cookie 已切换为账号 B
    那么导入被稳定拒绝且两个账号都没有新增会话
