# 功能冻结与体验收尾

本轮面向短期工程实践交付。下一轮只做验收、必要缺陷修复与交付材料；不新增账户安全中心、邮件验证、密码找回、OAuth、管理员后台、Redis 或辩论持久化。

## 按用户顺序核查

| 操作 | 本轮开始状态 | 收尾范围 |
| --- | --- | --- |
| 首次进入、切换自由聊/辩论赛、登录入口 | 已实现；页头调整未提交 | 审查并独立提交既有页头设计 |
| 注册、登录恢复、退出 | 已实现，有状态切换测试 | 保持认证契约 |
| 游客本地聊天、登录后显式导入、云端恢复 | 已实现；账号绑定与原始快照核对已修复 | 保留完整快照及安全清理语义 |
| 打开云端历史、加载更早消息 | 已实现，分页与 SSE 有竞态回归测试 | 保持每页读取和主动翻页 |
| 归档、查找归档、恢复、删除 | 归档后无查找/恢复入口，形成操作阻断 | 补最小筛选与恢复，完善等待、错误及重试 |
| 辩论过程、裁决与语音 | 已实现；语音失败不应阻断文字结果 | 检查取消及语音失败回归 |
| 手机与桌面导航、表单、按钮 | 响应式布局已有 | 检查实际窄屏操作问题 |

## 云端历史使用

“最近会话”显示未归档会话；“已归档”显示归档会话，可打开、恢复或删除。恢复后在“最近会话”查看。删除仍需确认且不可撤销。

复用 `GET /api/v1/conversations?limit=50&include_archived=true`，在返回页中筛选 `archived_at`；若本页无归档但还有游标，提示用户继续加载。每次点击最多读取一页，不自动扫描全部会话。恢复复用 `PATCH /api/v1/conversations/:id` 的 `{ "archived": false }`。无新增后端接口、Schema 或数据库迁移。

切换筛选或离开账号时取消旧列表与操作；迟到响应不能导航或更新当前列表。操作等待期间禁用重复提交，失败保留会话并可重试。

## 本轮操作防护

- 中文输入法组合输入中的 Enter 不触发发送；正常 Enter 与 Shift+Enter 保持原行为。
- 游客开启新对话前确认清除本地历史，取消确认保留消息；停止生成显示可继续发送的提示。
- 导入组件与历史组件使用不同的 React key，保持各自账号切换时的生命周期。
- 聊天长文本允许换行；取消辩论后忽略迟到事件和完成结果，避免旧结果重新出现。
- 增加语音合成失败回归：文字裁决保留，播放按钮可重试，也可以开始下一场。

## 实际验证（2026-09-09 至 2026-09-10）

| 检查 | 结果 |
| --- | --- |
| `ruff check src tests scripts` | 通过 |
| `ruff format --check src tests scripts` | 43 个文件通过 |
| `mypy src --ignore-missing-imports` | 24 个源文件通过 |
| `pytest tests/unit -v` | 73 通过 |
| `pytest tests/integration -v` | 22 通过，专用一次性 SQLite |
| `behave tests/bdd` | 2 features、10 scenarios、42 steps 通过 |
| `pnpm test` | 18 个文件、117 个测试通过 |
| `pnpm typecheck`、`pnpm build` | 通过 |

集成测试最初在隔离工作目录执行时，两项迁移测试因找不到仓库 Alembic 配置失败；随后在仓库根目录使用显式指定的专用测试数据库重跑，22 项全部通过，包括空库升级和第一阶段数据保留。未使用开发数据库；默认 fixture 清理的路径在执行前确认为不存在。

浏览器使用独立端口、独立认证 Cookie 和一次性数据库，聊天与辩论均为 fake LLM，TTS 为明确返回失败的替身，没有调用真实 SiliconFlow 或 edge-tts：

- Chrome 验证游客聊天与注册后页头状态；后续在内置浏览器继续登录、导入、归档、已归档筛选、恢复、详情页刷新、云端续聊和退出。
- 手机宽度（375 px）完成聊天与账号操作，桌面宽度（1280 px）检查辩论表单和主要操作，未见页面横向溢出。未做真实手机设备验收。
- 辩论六条发言与裁决可见；模拟语音失败显示提示，结果保留，“再来一场”可重置。退出后游客页面为空，不重新出现已清理副本。
- 分页与 SSE 交错、导入竞态和迟到响应由本轮全套回归覆盖；本轮没有重复此前的完整双 Chrome 标签页竞态手工验收。

## 最后一轮限定事项

1. 在交付环境完成 PostgreSQL、Docker/Nginx 深链接刷新与迁移验收；当前仅有 SQLite 和 Vite 本地结果，不能替代部署验收。补验项目目标 Python 3.11（本机为 3.12.13）。
2. 经单独授权后进行真实 LLM/TTS 冒烟验收，确认实际语音可播放；本轮仅离线验证。
3. 汇总演示步骤、截图、测试报告、部署说明及已知限制；确认 PR 的组员 Review 与 CI。此前修复已建立 PR #21，本轮新增提交仅在本地，未推送或合并。
4. 只处理验收发现的必要缺陷，不继续新增功能。归档筛选复用含归档的分页列表，较早归档可能需多次主动加载；辩论刷新不恢复结果，符合不做辩论持久化的范围。

用户已有 `AGENTS.md`、审计文档和打包指南保持未提交。页头调整经本轮授权审查后已单独提交。
